# reference-command/pages

本目录是历史参考原型页，用于保留早期目标图、模块文案和静态契约样本。

本目录下的 `Command*.vue` 文件不挂载到生产路由。运行时页面以 `frontend/src/views/*` 为准，入口和管理壳层以 `frontend/src/layout/*` 为准。

如果后续需要迁移或删除这些文件，先同步仍读取本目录的静态契约测试和路线图文档。
