[pytest]
addopts = -m "not frontend_contract"
testpaths = tests
norecursedirs =
    scripts
    pytest-cache-files-*
cache_dir = .pytest_cache
markers =
    frontend_contract: frontend source contract checks kept out of the default backend suite
