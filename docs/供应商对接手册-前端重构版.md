# 供应商对接手册

## 当前页面入口

- 审阅端：`/manage/today`。
- 厂级看板：`/manage/factory`。
- 车间看板：`/manage/workshop`。
- 手机填报：`/entry`。
- `/review/*` 与 `/admin/*` 保留兼容 redirect 到 `/manage/*`。

## 联调顺序

先联通登录、`/manage/factory` 与 `/entry`，再看移动端扫码、日报发布、AI 助手回答。

## 数据原则

页面只展示接口返回的真实数据。缺数据时显示缺口，不在前端伪造产量、能耗、成品率。
