# 企业微信生产入口准备清单

## 当前入口

- 浏览器可直接访问 `/entry` 与 `/manage/factory`。
- 手机填报入口：`https://<你的正式域名>/entry`。
- 审阅入口：`https://<你的正式域名>/manage/factory`。
- 旧 `/mobile` 可兼容跳转到 `/entry`。

企业微信正式入口先保持不接入，生产试跑优先走钉钉 H5、浏览器和系统账号。
